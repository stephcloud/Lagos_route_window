[project]
name = "lagos-route-window"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "stephanie", email = "stephchi@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "fastapi[standard]>=0.141.1",
]

[tool.uv]
package = false